services:
  # Backend Service (Spring Boot)
  - type: web
    name: trading-signal-backend
    rootDir: backend

  # AI Service (Flask + Gemma)
  - type: web
    name: trading-ai-service
    rootDir: ai-service
